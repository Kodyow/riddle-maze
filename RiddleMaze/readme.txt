Run this Application in Eclipse.

The Main function/entry point resided in the App.java file. 
This application uses JavaFX which will use additional changes to the run configuration.  
In the arguments tab "VM argument" paste below arguments.

--module-path "../references/javafx-sdk-17.0.1/lib" --add-modules javafx.controls